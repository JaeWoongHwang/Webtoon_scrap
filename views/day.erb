<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Today's Daum Webtoon</title>
  </head>
  <body>
    <a href="/">To Home</a>
    <table>
      <thead>
        <tr>
          <th>Image</th>
          <th>Title</th>
          <th>Introduction</th>
          <th>Score</th>
          <th>Link</th>
        </tr>
      </thead>
      <tbody>
        <% @webtoons.each do |webtoon| %>
        <tr>
          <!-- img url이 잘못 되서 못가져오는 경우에 alt에 있는 것을 출력하는 코드 -->
          <td> <img width="50" src="<%= webtoon[:img_url]%>" alt="<%= webtoon[:title]%>"> </td>
          <td><%= webtoon[:title]%></td>
          <td><%= webtoon[:desc]%></td>
          <!-- 반올림 할 수 있는 메소드 .round(올리는 자리수) -->
          <td><%= webtoon[:score].round(1)%></td>
          <!-- a 태그의 target 메소드를 통해 링크 클릭시 새창에서 볼 수 있다. -->
          <td> <a href="<%= webtoon[:url]%>" target = "_blank">Read Webtoon</a></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </body>
</html>
